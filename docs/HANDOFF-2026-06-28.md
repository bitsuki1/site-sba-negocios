# HANDOFF honesto — Site SBA Negócios (2026-06-28)

> Estado real ao encerrar a sessão da frente MKT/site (instância `claude/frontend-instance-sync-0me8pu`).
> Honesto: inclui o que está pronto, o que é gated em terceiros e os riscos/ressalvas. Sucede o `HANDOFF-2026-06-27.md`
> (preservado). Repo: `bitsuki1/site-sba-negocios`.

## 0. TL;DR
O site **saiu de preview e está OFICIALMENTE NO AR, completo**: `https://sbanegocios.com.br` (HTTPS, `www`→apex).
Esta sessão levou tudo de preview→produção e adicionou anti-spam, e-mail de lead, SEO/perf/LGPD, polimento de copy e
consolidou o código na `main`. O que falta é **material/decisão do dono** (time/fotos/logos, razão social+CNPJ, 2 toggles).

## 1. O que está NO AR (produção)
- **`https://sbanegocios.com.br`** — HTTPS forçado (cert do GitHub), `http`→`https`, `www`→apex.
- **DNS (GoDaddy):** 4 registros A no apex (`185.199.108.153`/`.109.153`/`.110.153`/`.111.153`) + 4 AAAA (IPv6) + `CNAME www → bitsuki1.github.io`.
- **Deploy:** publicação **manual** via `bash scripts/deploy-producao.sh` (build base `/` + `CNAME`/`404.html`/`.nojekyll` → push force na branch `gh-pages`). O **Pages-via-Actions** está pronto (`.github/workflows/deploy-pages.yml`) mas **inativo** até 2 toggles de Settings (ver §4).

## 2. Entregue nesta sessão (2026-06-27→28)
- **Virada preview→produção** (DNS + `gh-pages` no domínio próprio).
- **Anti-spam** no formulário: honeypot (`hp`) + tempo mínimo (`elapsed_ms < 1500ms`) — validados na Edge `submit-lead` (v3), testado (bot/rápido não gravam; válido grava).
- **Aviso de lead por e-mail** (Resend → `eduardo@saobentoservicos.com.br`), `reply_to` = e-mail do lead. Verificado ponta a ponta.
- **SEO:** título/meta/OG/Twitter **por rota** (`src/lib/seo.ts`, aplicado no `Layout`) + OG/Twitter/canonical estáticos no `index.html` + `public/sitemap.xml` + `robots.txt`.
- **Performance:** lazy-load de rotas (bundle inicial **513→171 kB**); imagens recomprimidas **−36%** (`sharp`); `loading="lazy"`.
- **LGPD:** página `/privacidade` (controlador/encarregado, bases legais, transferência internacional, segurança, ANPD) + links no rodapé e no formulário.
- **Conversão/marca:** **WhatsApp flutuante** em todas as páginas + links no rodapé/Contato; **logo real da CSTR**; faixa de **prova social CSTR** (fatos atribuídos ao parceiro).
- **Copy/UX (auditoria):** Home (hierarquia de CTA, menos jargão, "A SBA orquestra"), Soluções (filtro Público/Privado), Contato (headline + SLA "1 dia útil"), Setor Público/Privado, Sobre (sem "PMO"), desafios mais específicos.
- **Scaffold do time:** `TIME` em `src/data/site.ts` + seção "Quem conduz" no Sobre (gated — não aparece vazia).

## 3. Onde editar (mapa)
- Conteúdo central: `src/data/site.ts` (contato, navegação, setores, soluções, `PARCEIRO_CSTR`, `TIME`) · `src/data/landings.ts`.
- SEO por rota: `src/lib/seo.ts` (mapa `SEO_MAP`). · Botão WhatsApp: `src/components/BotaoWhatsApp.tsx`.
- Imagens: `src/assets/` (logo, `cstr/`, `conaid/`). · OG: `public/og-image.png` (**placeholder — trocar por banner 1200×630**).
- Edge Function de leads: **NÃO está no repo** — vive no Supabase (projeto `gestao-integrada-dados`, ref `lbjudeifksyeqminwlto`), função `submit-lead`.

## 4. Pendências — gated no dono (MOU)
- **Razão social + CNPJ** de São Bento Serviços → completar o controlador na Política de Privacidade.
- **Material do time** (nome/cargo/bio/foto) → preencher `TIME` e a seção aparece.
- **Logos** ConAid/Dynacal (só temos o da CSTR) · **cases ConAid** · **banner OG 1200×630**.
- **Pages-via-Actions (opcional):** Settings → Actions → General → Workflow permissions = *Read and write*; Settings → Pages → Source = *GitHub Actions*. Depois disso, push na `main` faz auto-deploy. Até lá, deploy manual pelo script.
- **(Opcional)** mover a key do Resend para um secret do Supabase (hoje é fallback no servidor; D106 = risco aceito pelo dono).

## 5. Ressalvas honestas (riscos)
- **Política de Privacidade:** revisão feita em melhor esforço, **não revisada por advogado**; identificação legal do controlador **incompleta** (falta razão social+CNPJ).
- **Conteúdo de parceiros** (CSTR/ConAid/Dynacal): **atribuído aos parceiros, não auditado por 3º**. **NEOMAG = PD&I** (marcado como tal). Mantido por decisão do dono.
- **Preview social (OG):** é **do site inteiro** (estático no `index.html`), **não por página** — robôs de preview não rodam JS. Preview por rota exigiria pré-render/SSG.
- **Recompressão de imagens:** feita por `sharp` com parâmetros conservadores (JPG q80, PNG lossless/paleta, ≤1400px); **não verificada visualmente por humano** — originais preservados no histórico do git.
- **Honestidade tributária mantida:** número só com fonte (STF/SICONFI/piso); honorário "no êxito", sem % inventado; "valores sob consulta".
- **Marca:** site é **SBA Negócios**; **não** se usa "Ambiental" (D45). E-mail exibido/notificações = `@saobentoservicos.com.br` (decisão do dono — domínio próprio descartado para e-mail).

## 6. Consolidação git (estado ao encerrar)
- **Site:** `main` = trabalho consolidado (branch `claude/frontend-instance-sync-0me8pu` ↔ `main` = 0/0, tudo por fast-forward/UNIÃO, **sem force-push**). Produção viva na `gh-pages`.
- **Unidade** (`bitsuki1/sba-unidades-de-negocios`): frente reconciliada por UNIÃO nos ledgers (PENDENCIAS §4, START-HERE §9), instância **FECHADA** no `REGISTRO-DE-INSTANCIAS.md` (rito C-T-21). `main` em dia (0/0).

## 7. Como retomar
1. `npm install && npm run dev` (porta 8080; em ambiente sem IPv6, `--host 127.0.0.1`).
2. Editar conteúdo em `src/data/*` e páginas em `src/pages/*`.
3. Publicar: `bash scripts/deploy-producao.sh` (até ligar o Pages-Actions).
4. Leads novos chegam por e-mail e ficam em `sba_leads` (Supabase). Para mexer na lógica de captura/anti-spam/e-mail: função `submit-lead` no Supabase.
